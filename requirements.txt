numpy>=1.20.0
matplotlib>=3.5.0

# === Optional: Real LLM Trace Collection ===
# Install these only when running scripts/collect_real_trace.py
# Requires CUDA-capable GPU with >=24GB VRAM (or use --allow-cpu-offload)
#   pip install torch>=2.1.0 transformers>=4.36.0 accelerate>=0.25.0
torch>=2.1.0
transformers>=4.36.0
accelerate>=0.25.0
